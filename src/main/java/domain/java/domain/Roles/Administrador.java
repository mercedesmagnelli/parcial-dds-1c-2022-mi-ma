package domain.java.domain.Roles;

import domain.java.domain.Roles.Rol;

public class Administrador extends Rol {

    public Administrador(String nombre, String apellido, String mail, String tel) {
        super(nombre, apellido, mail, tel);
    }
}
